検証がすべて完了しました。<code> タグの増減はいずれも意図的な改善(バッククォート表記の修正など)で、コマンド例の中身・デザイン・構造はすべて変更前と一致しています。

完了報告

8つのHTMLページ全ての文章を書き直し、app.js のクイズ採点メッセージも調整しました。デザイン(CSS・HTML構造・class・クイズの仕組み)には一切手を付けていません。

1. 不自然な改行の除去
- concepts.html はフォーマッタの影響で文の途中にソース改行が多数入っており、ブラウザ上で余計な空白として表示される状態でした。全ページで「1つの文・段落はソース上も1行」に統一し、検証コマンドで文中改行がゼロであることを確認済みです。

2. 自然な日本語への書き直し(全ページ)
- 「頭へ入れて」→「頭に入れて」、「step by step で進められ」→「一歩ずつ進められ」など、翻訳調・直訳調の表現を自然な日本語に修正
- 「Switchが中心になって転送されます」のような曖昧な受け身表現を、「Router を経由せず、Switch の転送だけで相手に届きます」のように動作が明確な文に変更
- 和文と英単語の間のスペースを本文全体で統一

3. 分かりやすさの向上(内容の正確化を含む)
- l3-switch-svi.html: 本文に生のバッククォート(`ip routing`)がそのまま表示されていたのを <code> タグに修正
- cisco-cli.html: 「CLI学習のコツ」にあった実在しない Router# save という表記を、実際のコマンドの流れ(enable → show → configure terminal → copy running-config startup-config)に修正
- 「no shutdown をしないと通信できない理由」「戻り経路が必要な理由」「trunk が必要な理由」など、初学者がつまずく箇所の説明を具体的に補強
- クイズの解説文も、なぜその答えになるのかが伝わる文に改善

検証結果
- class・data属性・href などの属性: 全ファイルで変更前後一致(クイズ・テーマ切替・ナビの動作に影響なし)
- code-block 内の Cisco コマンド例: 全ファイルで変更前後一致(手順の技術的内容は不変)
- 文中のソース改行・生バッククォート: 残存ゼロ
検証まで完了しました。既存の 01〜08 に続くステップアップ単元として、新しい2単元を追加し、全ページを新構成(全10単元)に更新しました。

追加した教材

09. OSPF 入門(ospf.html)— 動的ルーティングへの第一歩
- 中級ラボの静的ルーティング(Router 2台)から難易度を一段上げ、Router 3台+/30 リンクの構成で「静的ルートだと6本手書きが必要」という限界をまず体感させる導入にしています
- ワイルドカードマスク、router ospf / network / router-id の設定、show ip ospf neighbor での FULL 確認までを6ステップで解説
- 仕上げにリンクを意図的に shutdown して経路の自動再計算を観察する演習付き。失敗パターン(ワイルドカード誤り・area 不一致)も既存ページと同じ「If It Fails」形式で整理しました

10. 応用ラボ(advanced-labs.html)— DHCP・NAT・ACL+総合チャレンジ
- 「自宅のルーターがやっていることを自分で作る」をテーマに、1つのトポロジ(社内 LAN — R1 — ISP — Server)へ Lab A: DHCP → Lab B: NAT(PAT) → Lab C: 拡張 ACL を順に積み上げる構成です
- ACL の「暗黙の deny」や NAT の戻り経路など、初学者が必ずつまずく点を立ち止まって考えるコラムで補強しています
- 最後は手順書なしで要件と合格チェックリストだけを提示する「総合チャレンジ」で、VLAN・SVI・OSPF・DHCP・NAT・ACL の全単元を結びつけます

既存ページの更新

- 全10ページのサイドバーに 09・10 のナビゲーションを追加(既存の各ページの記法・インデントに合わせて統一)
- troubleshooting.html のフッターを「次のレッスン: OSPF 入門」に変更し、学習パスを連結
- index.html の統計(10ページ・9チェック・40+コマンド)、ロードマップ、ページ一覧、到達目標を新構成に合わせて更新

クイズは既存の app.js の仕組みをそのまま利用しており(新単元は各3問)、検証の結果、全ページのナビ数・リンク・HTML構造に問題はありません。デザイン(CSS)には手を加えていません。コミットが必要でしたらお知らせください。

すべての検証が通りました。単元8と9の間に新しい章「09. ACL入門」(acl.html)を追加し、全11ページの構成に更新しました。

新章「ACL入門」の内容

PC 2台 + Switch + Router + Web Server の小さなトポロジで、「PC-A だけ止まり PC-B は通る」という選別を目で確かめられる構成にしました。

- 2つのタイプを両方体験: 番号で指定する標準 ACL(access-list 10 deny host ...)と、名前で指定する拡張 ACL(ip access-list extended BLOCK-A-WEB)を1つずつ作ります。番号の範囲(1〜99 / 100〜199)、名前付きなら行単位の削除・挿入(no 10)ができる利点も解説しています。
- コマンドの意味を1つずつ解説: 各 STEP で deny host と ワイルドカードマスクの関係、permit any がないと暗黙の deny で全滅する理由、ip access-group の意味を、コード例の直後で説明しています。
- 効果を実感する仕掛け: 「作っただけでは何も起きない」→適用して止まる瞬間の Destination host unreachable(タイムアウトとの違いも説明)→ show access-lists のヒット数が増える様子、という流れです。拡張 ACL では「Ping は通るのに Web だけ止まる」を体験します。

in / out の重点解説

専用セクションを設け、さらにハンズオンの中心に据えました。

- 「主人公は Router」という判定原則と、Ping 1往復で通過する4つの検査ポイント(行き G0/0 in → G0/1 out、帰り G0/1 in → G0/0 out)を図解
- STEP 4 で「わざと間違える実験」: 初学者がやりがちな「PC-A は LAN 側だから G0/0 の out」を実際に試すと Ping が通ってしまうことを確認し、帰りパケットの送信元は Server だから deny に一致しない、という理由まで追いかけます。STEP 5 で in に付け替えて結果が反転するのを確認し、理解度チェックの Q1 でも同じ観点を出題しています。

既存ページの更新

- 全ページのサイドバー: ACL を 09 に挿入し、OSPF→10、応用ラボ→11 に繰り下げ(各ファイルの単行/複数行の記法に合わせて統一)
- 学習パスの連結: トラブルシュートの「次へ」→ ACL入門、OSPF の「前へ」→ ACL入門
- ospf.html・advanced-labs.html: ワイルドカードマスクや Lab C(ACL 実践)の記述を「ACL 入門で学んだ」を踏まえた表現に更新、総合チャレンジの単元番号参照も修正
- index.html: 統計(11ページ・7演習・10チェック)、ロードマップ、ページ一覧、到達目標に ACL を反映

検証スクリプトで、全11ページのナビ件数と番号、リンク切れなし、クイズが app.js の仕組み(3問・採点・解説表示)に適合していること、使用 CSS クラスがすべて styles.css に存在することを確認済みです。コミットが必要でしたらお知らせください。
j
すべての変更が完了し、検証も通りました。

変更の概要

教材はルーターの設定例が GigabitEthernet0/0・0/1 表記なのに機種が「任意の Router」と非指定だったため、推奨機種を ISR 1941(G0/0・G0/1 の2ポート搭載。全ラボは最大2ポートしか使わないので十分。2911 でも代用可)として全ページに明示しました。

packet-tracer.html(中心的な追記)
- 新セクション「装置の選び方とポート名の関係」を追加。FastEthernet(100Mbps・Fa0/1)と GigabitEthernet(1Gbps・G0/0)というポートの種類の違い、「同じ 0/1 でも種類が違えば別ポート」であること、推奨装置の対応表(Router → 1941、L2 Switch → Catalyst 2960、L3 Switch → Catalyst 3560)を解説
- 「別の機種を選んでしまったら」の callout で、1841/2811 なら fastEthernet0/0、ISR4331 なら gigabitEthernet0/0/0 への読み替え方と、show ip interface brief での確認方法を案内
- 装置配置の手順(STEP 2)を「任意の Router」から「1941 を選ぶ」に変更

各ハンズオンページ(lab、cisco-cli、intermediate-labs、troubleshooting、acl、ospf、advanced-labs の7ページ)
- トポロジ図の機種名を「Router」から「ISR 1941」に更新(advanced-labs は「社内Router (1941)」)
- 「今回の構成」セクション末尾に callout 注記を追加。各単元の内容に合わせて文面を調整しています(例: intermediate-labs ではサブインターフェースが fastEthernet0/0.10 になる点、advanced-labs では NAT コマンドのポート名、acl では「ポート名の取り違えが結果に直結する」点に言及)

l3-switch-svi.html
- 既存の「3560 系を選ぶ」ヒントに、3560 の PC 向けポートが FastEthernet(Fa0/1〜)である旨を一文補足

検証として、「任意の Router」と図中の汎用「Router」表記が残っていないこと、7ページすべてに注記が1つずつ入ったこと、全9ファイルの HTML タ

3つの依頼をすべて完了しました。ヘッドレスChromeで修正前後を描画比較し、縦書きバグの解消と「見た目が変わっていないこと」を確認済みです。

1. 上級者向けの単元を3つ追加(単元12〜14)

CCNA〜実務レベルで不足していたテーマを、既存教材と同じ構成(構成図・設定例・ミニラボ・理解度チェック・前後ページリンク)で追加しました。

- redundancy.html「冗長化」 — STP(ルートブリッジ選出・PortFast)、EtherChannel(LACP/PAgP)、HSRP(仮想IP・priority・preempt)。「わざと壊して切り替わりを観察する」ラボ付き。
- ipv6.html「IPv6入門」 — アドレスの省略ルール、アドレス種別(GUA・リンクローカル・マルチキャスト)、SLAAC、ipv6 unicast-routing、静的ルートとOSPFv3。
- security-ops.html「セキュリティと運用」 — SSH化(鍵生成の順序)、ポートセキュリティ(sticky・err-disabled復旧)、Syslog・NTP・設定バックアップ。

あわせて既存11ページすべてのサイドバーに3単元を追加し、index.html の統計(14ページ/13チェック)・ロードマップ・レッスン一覧・到達目標、advanced-labs.html のフッター(最終単元ではなくなったため次レッスンへのリンクに変更)を更新しました。

2. 「のように表記。コマンドでは」の縦書きバグ修正

原因はCSS側でした。.packet-list li などが display: flex だったため、<code> と <code> に挟まれたテキストが独立したflexアイテムに分割され、狭い幅で1文字ずつ縦に折り返されていました。liを通常のブロック表示に戻し、行頭のドットを position: absolute で描画する方式に変更。HTMLは無修正で、全ページの同種リストが直っています。

3. CSSのマジックナンバー削減

styles.css の数値を :root のデザイントークンに集約しました(値はすべて従来と同一)。

- スペーシング: --space-3xs(2px)〜--space-5xl(34px)の11段階
- 角丸: --radius-2xs〜--radius-hero + --radius-pill
- レイアウト/部品サイズ: --shell-pad、--sidebar-width、--size-nav/step/timeline、トグル関連など
- 関連する値は計算で導出(例: サイドバーの max-height: calc(100vh - var(--shell-pad) * 2)、トグルつまみの移動量は幅・枠線・余白からの calc())。トークンを1つ変えるだけで整合を保ったまま調整できます。なお、メディアクエリの条件にはCSS変数が使えないため、ブレークポイント(1180/880/640px)のみ数値のままです。

検証は、旧HTML+旧CSSと旧HTML+新CSSのスクリーンショット比較(バグ箇所以外は同一の見た目)、全14ページのHTMLタグ整合チェック、新規ページの描画確認まで実施しました。コミットはしていないので、内容を確認のうえ必要ならお申し付けください。グ対応が崩れていないことを確認済みです。コミットが必要であれば言ってください。

1. intermediate-labs.html(構成A: Router-on-a-Stick) — 図では PC-B が R1 に直結していましたが、本文の手順では「PC 2台とも SW1 に接続し(Fa0/1・Fa0/2)、Switch から Router へは trunk 1本」です。HSRPと同じT字型レイアウトに直し、PC-B を SW1 の下にぶら下げました。ポート番号(Fa0/1・Fa0/2)も併記しています。
2. troubleshooting.html(単元トポロジ) — R1 の G0/1 が 10.0.0.1 /30 なのに、その先に PC-B(192.168.20.10、GW 192.168.20.1)が直結という矛盾がありました。GW の 192.168.20.1 が図のどこにも存在しない状態です。ページ内に R2 への言及は一切ないため、R1 G0/1 を 192.168.20.1 /24 に修正して整合させました。
3. l3-switch-svi.html(冒頭の図) — 「VLAN間ルーティング(ip routing)」という装置ではない概念が、物理チェーンの中に1つのノードとして挟まっていました。PC-A — MLS1 — PC-B の3ノード構成に直し、ip routing で L3 有効 は MLS1 ノード内の注記に移しました。
4. security-ops.html(Mini Lab) — 「R1 + Server」が1ノードに合体してチェーンの端に置かれていましたが、実際は R1 も Server も SW1 に並列接続です(Server は 192.168.10.100 で PC-A と同一セグメント)。R1 と Server を分離し、Server を SW1 の下に配置するT字型にしました。

問題なしと判断したもの: ospf.html・ipv6.html・concepts.html の「PC を Router に直結」する図は、本文のラボ手順も Switch を使わない構成で一貫しており、Packet Tracer 上も有効な接続なのでそのままにしています。acl.html・advanced-labs.html のサーバー直結も同様です。lab.html・cisco-cli.html・packet-tracer.html は単純な直列構成で正しいままでした。

T字型のレイアウトは前回 styles.css に追加した .diagram-link.vertical と .diagram-center をそのまま再利用しているので、CSSの追加変更はありません。修正した3ページ(intermediate-labs・security-ops)のT字図がモバイル幅でも正しく1カラムに畳まれる点も既存の media query でカバーされています。
